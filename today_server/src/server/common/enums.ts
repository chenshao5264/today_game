export enum UserSate {
    STATE_NULL = 0,
    STATE_LOGIN,
    STATE_LOBBY,
    STATE_ROOM,
    STATE_READY,
    STATE_GAMING,
    STATE_BROKEN,
}

export enum RoomrSate {
    STATE_NULL = 0,
    STATE_EMPTY,
    STATE_READY,
    STATE_GAMING,
}

